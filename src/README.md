# SPDX-License-Identifier: Apache-2.0
<!-- This file was modified with the assistance of an AI (Large Language Model). Review required for correctness, security, and licensing. -->

# Source Layout Migration Staging Area

This directory is reserved for the planned `src/` layout migration.

Current status:
- Runtime package path is still `rackwright/`.
- Quality gates (lint/type/header checks) are now enabled first.
- Package move into `src/rackwright/` will be performed in a dedicated follow-up change.
